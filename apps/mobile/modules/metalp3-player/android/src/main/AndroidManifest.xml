<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <meta-data
      android:name="com.google.android.gms.car.application"
      android:resource="@xml/automotive_app_desc" />

    <service
      android:name="expo.modules.metalp3player.PlaybackService"
      android:exported="true"
      android:foregroundServiceType="mediaPlayback">
      <intent-filter>
        <action android:name="androidx.media3.session.MediaLibraryService" />
        <action android:name="android.media.browse.MediaBrowserService" />
      </intent-filter>
    </service>

    <receiver
      android:name="expo.modules.metalp3player.widget.PlaybackWidgetProvider"
      android:exported="true"
      android:label="@string/metalp3_widget_label">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="expo.modules.metalp3player.widget.PLAY_PAUSE" />
        <action android:name="expo.modules.metalp3player.widget.NEXT" />
        <action android:name="expo.modules.metalp3player.widget.PREV" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/metalp3_playback_widget_info" />
    </receiver>
  </application>
</manifest>
